# 坐标轴优化说明

## 优化内容

已成功优化 `plot_results.py` 中的坐标轴范围，使其更加合理和美观。

## 主要改进

### 1. X轴范围优化
- **之前**: `ax.set_xlim(5, 100)` - 范围过大，数据点分布稀疏
- **现在**: `ax.set_xlim(4, 60)` - 更紧凑的范围，突出显示数据点

### 2. Y轴范围优化
- **之前**: `ax.set_ylim(0, 200)` - 范围过大，数据点分布稀疏
- **现在**: `ax.set_ylim(50, 150)` - 更紧凑的范围，突出显示差异

### 3. 刻度设置优化
- **X轴刻度**: 设置为 `[5, 7, 9, 12, 15, 20, 30, 50]` - 对应实际数据点
- **Y轴刻度**: 设置为 `[50, 60, 70, 80, 90, 100, 110, 120, 130, 140, 150]` - 合理的FID值范围

## 优化效果

### 视觉效果改进
- **数据点更集中**: 所有数据点都在图表中心区域
- **差异更明显**: 不同方法之间的FID差异更容易观察
- **刻度更合理**: 刻度标签对应实际数据点

### 论文质量提升
- **更专业**: 符合学术论文的图表标准
- **更清晰**: 数据趋势更容易理解
- **更美观**: 整体视觉效果更佳

## 技术细节

### 普通FID结果图
```python
# 设置x轴为对数刻度
ax.set_xscale('log')
ax.set_xlim(4, 60)  # 更合理的x轴范围

# 设置y轴范围
ax.set_ylim(50, 150)  # 更合理的y轴范围，突出显示差异

# 设置x轴刻度
ax.set_xticks([5, 7, 9, 12, 15, 20, 30, 50])
ax.set_xticklabels([5, 7, 9, 12, 15, 20, 30, 50])

# 设置y轴刻度
ax.set_yticks([50, 60, 70, 80, 90, 100, 110, 120, 130, 140, 150])
ax.set_yticklabels([50, 60, 70, 80, 90, 100, 110, 120, 130, 140, 150])
```

### 对数尺度FID结果图
```python
# 设置x轴为对数刻度
ax.set_xscale('log')
ax.set_xlim(4, 60)  # 更合理的x轴范围

# 设置y轴为对数刻度
ax.set_yscale('log')
ax.set_ylim(50, 150)  # 更合理的y轴范围，突出显示差异

# 设置刻度（同上）
```

## 数据范围分析

### FID值分布
- **最低值**: 约61.54 (HILDA方法在5步时)
- **最高值**: 约142.39 (PNDM方法在5步时)
- **主要范围**: 60-140之间
- **优化后范围**: 50-150，包含所有数据点并留有余量

### 步数分布
- **实际步数**: [5, 7, 9, 12, 15, 20, 50]
- **优化后范围**: 4-60，包含所有步数并留有余量
- **对数刻度**: 更好地展示步数变化

## 使用建议

1. **论文使用**: 建议使用优化后的图表，视觉效果更佳
2. **数据展示**: 可以清楚看到HILDA方法在所有步数下的优势
3. **趋势分析**: 不同方法的性能趋势更容易观察
4. **对比分析**: 方法之间的差异更加明显

## 文件输出

优化后的脚本会生成：
- `celeba_fid_results_iclr_{timestamp}.png` - 普通FID结果图
- `celeba_fid_results_iclr_{timestamp}.pdf` - PDF格式（适合论文）
- `celeba_fid_results_log_iclr_{timestamp}.png` - 对数尺度FID结果图
- `celeba_fid_results_log_iclr_{timestamp}.pdf` - PDF格式
- `celeba_fid_report_{timestamp}.txt` - 详细分析报告

