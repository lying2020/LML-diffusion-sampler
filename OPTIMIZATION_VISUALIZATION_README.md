# 优化算法3D可视化项目

## 项目概述

这个项目实现了最速下降法和共轭梯度法的3D可视化，特别展示了最速下降法的"锯齿形"（zigzag）现象，并通过2D投影清晰地显示了优化路径。

## 主要特性

### 1. 算法实现
- **最速下降法（Steepest Descent）**：使用负梯度方向作为搜索方向
- **共轭梯度法（Conjugate Gradient）**：使用Polak-Ribiere公式计算β系数
- **精确线搜索**：对于二次函数使用解析解
- **回溯线搜索**：对于非二次函数使用Armijo条件

### 2. 可视化功能
- **3D表面图**：显示目标函数的3D表面
- **等高线投影**：在底部显示2D等高线
- **优化路径**：3D空间中显示算法迭代路径
- **2D投影**：半透明虚线显示2D投影，清晰展示zigzag现象
- **梯度向量**：显示每步的梯度方向
- **收敛分析**：函数值和梯度范数的收敛曲线

### 3. 测试函数
- **二次函数**：`f(x) = 0.5 * x^T A x - b^T x`
- **Rosenbrock函数**：经典的非凸优化测试函数
- **Rastrigin函数**：多模态函数，具有许多局部最优
- **Ackley函数**：另一个多模态测试函数

## 文件结构

```
├── test_gradient_descent.py              # 原始2D版本
├── gradient_descent_2d_visualization.py     # 优化2D版本（英文）
├── test_gradient_descent_3d.py           # 3D版本（有Arrow3D问题）
├── test_gradient_descent_3d_enhanced.py  # 增强3D版本
├── gradient_descent_3d_visualization.py     # 修复版3D版本（推荐使用）
└── OPTIMIZATION_VISUALIZATION_README.md  # 本说明文件
```

## 使用方法

### 运行3D可视化（推荐）
```bash
python3 gradient_descent_3d_visualization.py
```

### 运行2D可视化
```bash
python3 gradient_descent_2d_visualization.py
```

## 主要发现

### 1. 最速下降法的Zigzag现象
- 在二次函数上，最速下降法表现出明显的锯齿形路径
- 2D投影清晰显示了相邻梯度向量的正交性
- 这种现象在条件数较高的函数上更加明显

### 2. 共轭梯度法的优势
- 对于二次函数，共轭梯度法在n步内收敛（n为维数）
- 避免了最速下降法的zigzag现象
- 在非二次函数上也有更好的收敛性能

### 3. 不同函数的性能对比
- **二次函数**：CG明显优于SD（2步 vs 20步）
- **Rosenbrock函数**：两种算法都需要更多迭代，CG略优
- **Rastrigin函数**：从全局最优附近开始，两种算法都快速收敛
- **Ackley函数**：SD收敛到全局最优，CG陷入局部最优

## 交互功能

### 3D图形交互
- **旋转**：拖拽鼠标旋转3D视图
- **缩放**：鼠标滚轮缩放
- **平移**：右键拖拽平移视图
- **2D投影**：观察底部的半透明投影线，清晰看到zigzag模式

### 分析图表
- **收敛曲线**：函数值和梯度范数的对数尺度图
- **Zigzag分析**：角度分析和步长分析
- **性能对比**：详细的数值比较

## 技术特点

### 1. 代码结构
- 面向对象设计，易于扩展
- 模块化函数，便于维护
- 支持多种测试函数

### 2. 可视化技术
- Matplotlib 3D绘图
- 自定义颜色和透明度
- 多子图布局
- 动态标注

### 3. 数值方法
- 精确线搜索（二次函数）
- 回溯线搜索（非二次函数）
- 数值稳定性处理

## 扩展可能

1. **添加更多算法**：BFGS、L-BFGS、Adam等
2. **更高维度**：扩展到3D以上的优化问题
3. **动画效果**：创建优化过程的动画
4. **交互式参数调整**：实时调整算法参数
5. **性能分析**：更详细的收敛性分析

## 依赖库

```python
numpy
matplotlib
mpl_toolkits.mplot3d
time
```

## 运行环境

- Python 3.6+
- 支持3D图形显示的matplotlib后端
- 建议在Jupyter Notebook或支持交互式图形的环境中运行

## 总结

这个项目成功展示了：
1. 最速下降法的zigzag现象及其3D可视化
2. 共轭梯度法的优越性能
3. 不同测试函数上的算法表现
4. 直观的2D投影帮助理解优化路径

通过3D可视化，我们可以更直观地理解优化算法的工作原理，特别是最速下降法的锯齿形收敛模式，这对于理解优化理论具有重要意义。
